services:
  - type: web
    name: svg2excalidraw
    runtime: python
    buildCommand: uv sync --no-dev
    startCommand: uv run uvicorn svg2excalidraw.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
